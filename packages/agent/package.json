{
  "name": "agent",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public --kv AGENT_PAYMENTS_KV --kv AGENT_CARDS_KV --kv AGENT_KEYS_KV",
    "deploy": "wrangler pages deploy public --project-name agent --commit-dirty=true",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250620.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.0.0"
  }
}
