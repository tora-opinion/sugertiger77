{
  "name": "image-protect",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public --r2 IMAGE_BUCKET --kv RATE_LIMIT_KV",
    "deploy": "wrangler pages deploy public --project-name image-protect --commit-dirty=true",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260823.1",
    "typescript": "^6.0.3",
    "wrangler": "^4.125.0"
  }
}
